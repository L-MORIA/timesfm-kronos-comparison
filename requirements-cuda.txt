# timesfm-kronos-comparison dependencies (CUDA — ONLY for sm_80+ GPUs)
# Python 3.12; install inside the project's .venv (NOT Hermes global venv):
#   uv venv .venv --python 3.12
#   uv pip install -r requirements-cuda.txt
#
# SUPPORTED: Ampere (RTX 30xx, A100, sm_80/86), Hopper (H100, sm_90)
# NOT SUPPORTED: Blackwell (RTX 50xx, sm_120) — kernel image missing in PyTorch nightly
# If you have RTX 5060 Ti or newer Blackwell — USE requirements.txt (CPU-only)

requests
numpy
pandas
matplotlib
torch --index-url https://download.pytorch.org/whl/cu121
einops
timesfm