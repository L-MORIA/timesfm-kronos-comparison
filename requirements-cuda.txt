# timesfm-kronos-comparison dependencies (CUDA)
# Python 3.12; install inside the project's .venv (NOT Hermes global venv):
#   uv venv .venv --python 3.12
#   uv pip install -r requirements-cuda.txt
#
# NOTE: Requires GPU with compute capability sm_80+ (Ampere: RTX 30xx, A100; Hopper: H100).
#       RTX 5060 Ti (Blackwell, sm_120) is NOT supported by current stable PyTorch.
#       For unsupported GPUs, fall back to requirements-cpu.txt.

requests
numpy
pandas
matplotlib
torch --index-url https://download.pytorch.org/whl/cu121
einops
timesfm