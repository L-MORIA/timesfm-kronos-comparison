# timesfm-kronos-comparison dependencies (CPU-only — default)
# Python 3.12; install inside the project's .venv (NOT Hermes global venv):
#   uv venv .venv --python 3.12
#   uv pip install -r requirements.txt
#
# This is the SAFE DEFAULT — works everywhere (CPU, any GPU, Blackwell sm_120).
# For CUDA acceleration on supported GPUs (Ampere sm_80+), use requirements-cuda.txt

requests
numpy
pandas
matplotlib
torch --index-url https://download.pytorch.org/whl/cpu
einops
timesfm